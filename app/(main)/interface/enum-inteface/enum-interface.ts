
// Para Receber os valores dos Enum retornados do backend API
// A mesma interface é usada para os enums de 
// Status,Sex,Tipos de Pagamentos,Tipos de Pessoas etc..
export interface StatusValues {
    value: string;
    label: string;
}
